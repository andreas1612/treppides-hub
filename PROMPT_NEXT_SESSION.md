# New Session Prompt — Paste this to start

---

Continue the Treppides Hub project. You are already on the server (192.168.0.221) — no SSH needed.

Before touching any code, read:
1. `NEXT_SESSION.md` — what was done last + current priorities
2. `PROJECT_BRIEF.md` — full project context, stack, file structure, rules
3. `STATUS.md` — live service status, known issues, what's pending

Repo is at `~/treppides-hub`. Stack is vanilla HTML/CSS/JS (no build step). Deploy = `git add -A && git commit && git push`, then hard-refresh browser.
